name: Deploy to Render

on:
  push:
    branches: [ "main" ]   # trigger when you push to main
jobs:
  deploy:
    runs-on: ubuntu-latest  # use a Linux machine
    steps:
    - name: Checkout code   # download your code
      uses: actions/checkout@v3
    - name: Dummy step      # just print a message
      run: echo "Code pushed successfully!"